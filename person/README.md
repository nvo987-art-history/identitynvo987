# Person Record

This directory contains versioned JSON-LD representations of a person entity.

Each version folder stores a snapshot of the `person.jsonld` document for that
specific version. The purpose of this structure is to maintain a transparent
history of updates while preserving previous records.

The files are intended for use in linked data contexts and structured data
applications.

Archive created: 2026-03-22
